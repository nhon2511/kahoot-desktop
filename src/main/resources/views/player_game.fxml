<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<AnchorPane xmlns="http://javafx.com/javafx/21"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.kahoot.client.PlayerGameController"
            prefWidth="800" prefHeight="600"
            style="-fx-background-color: #f5f5f5;">
    
    <VBox spacing="20" alignment="CENTER" AnchorPane.topAnchor="20" AnchorPane.leftAnchor="20" AnchorPane.rightAnchor="20" AnchorPane.bottomAnchor="20">
        <!-- Header -->
        <HBox spacing="20" alignment="CENTER">
            <Label fx:id="playerNameLabel" text="Player: " style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #7B2CBF;" />
            <Label fx:id="scoreLabel" text="Điểm: 0" style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #28a745;" />
            <Label fx:id="timerLabel" text="00:00" style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #dc3545; -fx-background-color: white; -fx-background-radius: 10; -fx-padding: 10 20;" />
            <Button text="Rời phòng" onAction="#handleLeaveRoomAction" style="-fx-background-color: #ef4444; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 6; -fx-padding: 8 12;" />
        </HBox>
        
        <!-- Question Info -->
        <Label fx:id="questionNumberLabel" text="Câu hỏi 1/10" style="-fx-font-size: 16px; -fx-text-fill: #666;" />
        
        <!-- Question Card -->
        <VBox spacing="20" alignment="CENTER" style="-fx-background-color: white; -fx-background-radius: 15; -fx-padding: 40; -fx-min-width: 700;">
            <Label fx:id="questionTextLabel" text="Câu hỏi sẽ hiển thị ở đây..." 
                   style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #333; -fx-wrap-text: true; -fx-text-alignment: center;" 
                   wrapText="true" />
            
            <Separator prefWidth="600" />
            
            <!-- Options Grid -->
            <GridPane fx:id="optionsGrid" hgap="15" vgap="15" alignment="CENTER">
                <columnConstraints>
                    <ColumnConstraints prefWidth="300" />
                    <ColumnConstraints prefWidth="300" />
                </columnConstraints>
            </GridPane>
        </VBox>
        
        <!-- Status Message -->
        <Label fx:id="statusLabel" text="Chờ câu hỏi tiếp theo..." 
               style="-fx-font-size: 14px; -fx-text-fill: #666;" />
        
        <!-- Leaderboard -->
        <VBox spacing="10" alignment="CENTER" style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 20; -fx-min-width: 500;">
            <Label text="🏆 Bảng xếp hạng" style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #7B2CBF;" />
            <ListView fx:id="leaderboardListView" prefWidth="450" prefHeight="200" 
                     style="-fx-font-size: 16px; -fx-font-weight: bold;" />
        </VBox>
        
        <!-- Waiting Screen -->
        <VBox fx:id="waitingBox" spacing="20" alignment="CENTER" visible="true">
            <Label text="⏳ Đang chờ host bắt đầu câu hỏi..." 
                   style="-fx-font-size: 20px; -fx-text-fill: #7B2CBF;" />
        </VBox>
    </VBox>
    
</AnchorPane>

